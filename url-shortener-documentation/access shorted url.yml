info:
  name: access shorted url
  type: http
  seq: 11

http:
  method: GET
  url: "{{backend-url}}/zuli"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
