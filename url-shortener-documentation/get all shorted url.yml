info:
  name: get all shorted url
  type: http
  seq: 7

http:
  method: GET
  url: "{{backend-url}}/all"
  body:
    type: json
    data: |-
      {
        "userID": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
